package.ATACurriculum_RecommendationsService = {
    interfaces = (1.0);

    build-system = brazil-gradle;
    build-tools = {
        1.0 = {
            ATACheckstyle = 1.1;
            JDK8 = 1.0;
            BrazilGradle = 5.x;
            BrazilGradleQualityDefaults = 5.x;
        };
    };

    dependencies = {
        1.0 = {
            AmazonCoralDaggerSupportRuntime = 2.x;
            ATACurriculum_JavaProjectResources = 1.0;
        };
    };

    test-dependencies = {
        1.0 = {
            # If only using JUnit 4, replace with 'Maven-junit_junit = 4.12.x;'
            # and remove 'useJUnitPlatform' from build.gradle. For mixed
            # JUnit 4 and 5 tests, keep this and add 'JUnit-4-12-migration = 5.x;'
            JUnit5 = 5.x;
        };
    };

    resolves-conflict-dependencies = {
        1.0 = {
        };
    };

    targets = {
        ATACurriculum_RecommendationsService-1.0 = { type = javalibrary; };
    };
};
